# SmartSketch-AI
SmartSketch-AI is a full-stack deep learning web application that recognizes handwritten digits in real time using a trained PyTorch neural network model. Users can draw digits on an interactive canvas, and the application instantly predicts the digit with confidence scores.
